import '@testing-library/jest-dom';

// Supabase is the primary database for the project.
// MongoDB is no longer used.

// Global setup for all test files
beforeAll(async () => {
  // Add global setup logic here if needed for Supabase testing
}, 10000);

afterAll(async () => {
  // Add global teardown logic here if needed
});
